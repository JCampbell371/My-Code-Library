  // For-of Loop
  // An easier way of looping through an Array in ES6 and 
  // grabbing the values of that array
  
  let testResults = [1.3, 3.2, 2, 44, 33];
  for(let testResult of testResults) {
   console.log(testResult); 
  }